<% @page_title = 'delete user' %>
<%= link_to("Back",{:action => 'index'}, :class => 'back-link') %>
<div class = "User delete">
	<h2>Create Subjects</h2>
	<%= form_for(:subject, :url => {:action => 'destroy' , :id => @users.id}) do |f| %>
	<table>
		<tr>
			<th>Are You Sure You want delete?</th>
			<td><%= @users.user_name %></td>
		</tr>
	</table>
	<div class="form-buttons">
		<%= submit_tag("Delete '#{@users.user_name}'") %>
	</div>	  
	<% end %>
</div>
